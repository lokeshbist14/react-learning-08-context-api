import React, { createContext } from "react";
// createContext() is used to create a Context that allows data to be shared between React components without passing props manually.

const UserContext = createContext();

export default UserContext;
